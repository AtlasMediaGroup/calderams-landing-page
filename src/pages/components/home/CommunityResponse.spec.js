import React from 'react'
import { shallow } from 'enzyme'
import CommunityResponse from './CommunityResponse'

describe('CommunityResponse', () => {
  it('shallow renders', () => {
    shallow(<CommunityResponse />)
  })
})
